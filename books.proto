syntax = "proto3";

package books;

service BookService {
  rpc List (Empty) returns (BookList);
  rpc Insert (Book) returns (Empty);
}

message Empty {}

message Book {
  string title = 1;
  string author = 2;
}

message BookList {
  repeated Book books = 1;
}
